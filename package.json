{
  "name": "homi",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "packageManager": "pnpm@10.34.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "generate:face-assets": "node scripts/generate-face-assets.mjs",
    "build": "vite build",
    "check": "node --check public/app.js && node --check scripts/generate-face-assets.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
